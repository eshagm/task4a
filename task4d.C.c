#include<stdio.h>

main()
{
	float pi,r,area;
	printf("thr number pi=",&area);
	scanf("%f",&pi);
	printf("thr number r=",&area);
	scanf("%f",&r);
	area=2*pi*r;
	printf("area of pc=%f",area);
	
}
